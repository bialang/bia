let mut x = "hi"
// print(x)
// hello_world(1 and 0)
// let n = compare(x, "hi")
// hello_world(n)
x = "no"
let x = ""
if compare(x, "no") == 0 {
	print("is the same")
} else {
	print("not the same")
}
