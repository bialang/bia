cmake_minimum_required(VERSION 3.1)

# Project info
project(Bia)
set(Bia_VERSION_MAJOR 3)
set(Bia_VERSION_MINOR 60)

include_directories(Bia)

add_subdirectory(Bia)

#add_executable(Bia Bia/main.cpp)
#add_library(bia ${Bia_HEADER_FILES_COMPILER} ${Bia_SOURCE_FILES_COMPILER} ${Bia_HEADER_FILES_EXCEPTION} ${Bia_HEADER_FILES_FORCE} ${Bia_HEADER_FILES_FRAMEWORK} ${Bia_HEADER_FILES_EXECUTABLE} ${Bia_HEADER_FILES_NATIVE} ${Bia_SOURCE_FILES_NATIVE} ${Bia_HEADER_FILES_OBJECT} ${Bia_HEADER_FILES_GRAMMAR} ${Bia_SOURCE_FILES_GRAMMAR} ${Bia_HEADER_FILES_MACHINE} ${Bia_SOURCE_FILES_MACHINE} ${Bia_HEADER_FILES_ARCHITECTURE} ${Bia_HEADER_FILES_DISASSEMBLER} ${Bia_SOURCE_FILES_DISASSEMBLER} ${Bia_HEADER_FILES_LINK} ${Bia_SOURCE_FILES_LINK} ${Bia_HEADER_FILES_MODULAR} ${Bia_SOURCE_FILES_MODULAR} ${Bia_HEADER_FILES_STREAM} ${Bia_SOURCE_FILES_STREAM} ${Bia_HEADER_FILES_UTILITY})

add_executable(testa test.cpp)
set_target_properties(testa PROPERTIES COMPILE_DEFINITIONS BUILDER_STATIC_DEFINE)
target_link_libraries(testa Bia)
enable_testing()
add_test(testa testa)