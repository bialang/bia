import io
import sys

for i in r"\d+".match_all("02.08.2020") {
	io.print(i.group(0))
}

// create on the stack
let client = Client{} // type: Client

// create on the heap (automagically freed)
let client = new Client{} // type: *Client
